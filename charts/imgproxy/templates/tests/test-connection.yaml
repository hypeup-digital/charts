apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imgproxy.fullname" . }}-test-connection"
  labels:
    {{- include "imgproxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "imgproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
